import { AreaOfcirclePipe } from './area-ofcircle.pipe';

describe('AreaOfcirclePipe', () => {
  it('create an instance', () => {
    const pipe = new AreaOfcirclePipe();
    expect(pipe).toBeTruthy();
  });
});
